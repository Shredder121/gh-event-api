language: java
jdk:
  - oraclejdk8

#validate javadoc
before_install: mvn -B -q javadoc:javadoc -Dshow=package

install: mvn -B -q install -DskipTests=true
script: mvn -B test

cache:
  directories:
    - $HOME/.m2
